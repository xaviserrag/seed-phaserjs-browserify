{
  "name": "seed-hot-loading",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "browserify" : "browserify -p [ partition-bundle --map mapping.json --output build/src ]",
    "grunt": "grunt copy",
    "build" : "grunt copy && npm run browserify"
  },
  "author": "Xavi Serra",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-browserify": "^3.5.0",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.8.0",
    "grunt-http-server": "^1.2.0",
    "partition-bundle": "^1.0.14"
  }
}
